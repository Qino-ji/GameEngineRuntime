# 01 Clear Screen

Minimal clear-screen smoke app for GameEngineRuntime.

## Run

```powershell
cd runtime
dub build --build=debug --force
.\ger_runtime.exe
```

Expected result: an 800x600 window opens and the runtime stays alive until closed.
